import React from 'react'

function LoadingMoreButton(loadMore, loadingMore) {
  if (loadingMore === true) {
    return <p>Loading...</p>
  } else {
    return (
      
    )
  }
}

export default LoadingMoreButton;